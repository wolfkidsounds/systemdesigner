<form method="post" action="{{ path('app_processor_delete', {'id': processor.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ processor.id) }}">
    <button class="btn text-danger"><i class="fa-solid fa-trash"></i></button>
</form>
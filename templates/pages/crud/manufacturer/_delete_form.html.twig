<form method="post" action="{{ path('app_manufacturer_delete', {'id': manufacturer.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manufacturer.id) }}">
    <button class="btn text-danger"><i class="fa-solid fa-trash"></i></button>
</form>
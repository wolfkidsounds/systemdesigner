{{ form_start(form) }}
<div class="d-flex flex-column overflow-hidden">
        
    {{ include('pages/limiter/partials/_selector.html.twig') }}

    <div class="divider seperator mb-3"><hr></div>

    {{ include('pages/limiter/partials/_settings.html.twig') }}

    <div class="loading-container-wrapper p-3">

        <div class="loader-container" style="display:none;">
            <div class="custom-loader"></div>
        </div>

        <div class="custom-loader-item unloaded">

            {{ include('pages/limiter/partials/_calc_fields.html.twig') }}
            
            {{ include('pages/limiter/partials/_lim_times.html.twig') }}
            
            {{ include('pages/limiter/partials/_details.html.twig') }}

            <div class="divider seperator mb-3"><hr></div>

            {{ include('pages/limiter/partials/_messagebox.html.twig') }}
        </div>
    </div>

    <div class="divider seperator mb-3"><hr></div> {# Add a separator here #}

    <div class="row">
        <button class="btn btn-primary m-0 mb-0">{{ button_label|default('Save') }}</button>
    </div>

</div>
{{ form_end(form) }}
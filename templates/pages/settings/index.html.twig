{% extends 'content.html.twig' %}

{% block title %}{{ parent() }} | {{ 'Settings'|trans }}{% endblock %}

{% block page_content %}
    <div class="card">
        <div class="card-body">
        {{ include('pages/settings/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

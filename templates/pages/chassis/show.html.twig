{% extends 'base.html.twig' %}

{% block title %}Chassis{% endblock %}

{% block body %}
    <h1>Chassis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chassis.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chassis.Name }}</td>
            </tr>
            <tr>
                <th>Validated</th>
                <td>{{ chassis.Validated ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chassis_index') }}">back to list</a>

    <a href="{{ path('app_chassis_edit', {'id': chassis.id}) }}">edit</a>

    {{ include('chassis/_delete_form.html.twig') }}
{% endblock %}

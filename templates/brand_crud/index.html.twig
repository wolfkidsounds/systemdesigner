{% extends 'base.html.twig' %}

{% block title %}Brand index{% endblock %}

{% block body %}
    <h1>Brand index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
                <th>UserId</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for brand in brands %}
            <tr>
                <td>{{ brand.id }}</td>
                <td>{{ brand.Name }}</td>
                <td>{{ brand.Type }}</td>
                <td>{{ brand.UserId }}</td>
                <td>
                    <a href="{{ path('app_brand_crud_show', {'id': brand.id}) }}">show</a>
                    <a href="{{ path('app_brand_crud_edit', {'id': brand.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_brand_crud_new') }}">Create new</a>
{% endblock %}

{% extends 'layout.html.twig' %}

{% block title %}Processor{% endblock %}

{% block body %}
    <h1>Processor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ processor.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ processor.Brand }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ processor.Name }}</td>
            </tr>
            <tr>
                <th>ChannelsInput</th>
                <td>{{ processor.ChannelsInput }}</td>
            </tr>
            <tr>
                <th>ChannelsOutput</th>
                <td>{{ processor.ChannelsOutput }}</td>
            </tr>
            <tr>
                <th>OutputOffset</th>
                <td>{{ processor.OutputOffset }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_processor_index') }}">back to list</a>

    <a href="{{ path('app_processor_edit', {'id': processor.id}) }}">edit</a>

    {{ include('processor/_delete_form.html.twig') }}
{% endblock %}

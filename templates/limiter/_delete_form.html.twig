<form method="post" action="{{ path('app_limiter_delete', {'id': limiter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ limiter.id) }}">
    <button class="btn text-danger">Delete</button>
</form>

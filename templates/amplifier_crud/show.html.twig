{% extends 'base.html.twig' %}

{% block title %}Amplifier{% endblock %}

{% block body %}
    <h1>Amplifier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amplifier.id }}</td>
            </tr>
            <tr>
                <th>BrandId</th>
                <td>{{ amplifier.BrandId }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ amplifier.UserId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ amplifier.Name }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ amplifier.Height }}</td>
            </tr>
            <tr>
                <th>OutputChannels</th>
                <td>{{ amplifier.OutputChannels }}</td>
            </tr>
            <tr>
                <th>Power16</th>
                <td>{{ amplifier.Power16 }}</td>
            </tr>
            <tr>
                <th>Power8</th>
                <td>{{ amplifier.Power8 }}</td>
            </tr>
            <tr>
                <th>Power4</th>
                <td>{{ amplifier.Power4 }}</td>
            </tr>
            <tr>
                <th>Power2</th>
                <td>{{ amplifier.Power2 }}</td>
            </tr>
            <tr>
                <th>PowerBridge8</th>
                <td>{{ amplifier.PowerBridge8 }}</td>
            </tr>
            <tr>
                <th>PowerBridge4</th>
                <td>{{ amplifier.PowerBridge4 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_amplifier_crud_index') }}">back to list</a>

    <a href="{{ path('app_amplifier_crud_edit', {'id': amplifier.id}) }}">edit</a>

    {{ include('amplifier_crud/_delete_form.html.twig') }}
{% endblock %}
